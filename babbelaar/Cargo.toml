[package]
name = "babbelaar"
version.workspace = true
edition.workspace = true

[dependencies]
thiserror = "1"
strum = { version = "0.26", features = ["derive"] }

log = "0.4"

[dev-dependencies]
rstest = "0.23"
